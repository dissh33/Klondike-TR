pipeline {
  agent any
  
  stages {

    stage ('Clean Workspace') {
      steps {
        cleanWs()
      }
    }

    stage ('Docker Build') {
      steps { 
        docker build -t dissh33/items-sevice .
      }
    }

  }
}
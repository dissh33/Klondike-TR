pipeline {
  agent any
  
  stages {

    stage ('Clean workspace') {
      steps {
        cleanWs()
      }
    }

    stage ('Initialize') {
      steps { 
        echo 'Placeholder.'
      }
    }
    
  }
}